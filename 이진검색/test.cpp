/*
		¿Œº∫
*/

#include<stdio.h>

int q[10000];

int main() {
	int i, n, s;
	int st = -1, ed = -1;
	scanf("%d %d", &n, &s);
	for (i = 1; i <= n; i++) 
		q[++st] = i;
	printf("<");
	while (st != ed) {
		t = q[++ed];
		if (t == k) break;
		if(t-1>=0 && chk[t-1]==0){
			q[++st] = t - 1;
			via[st]=via[ed]

	return 0;
}